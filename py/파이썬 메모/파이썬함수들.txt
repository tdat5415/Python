print('asdf')
괄호안을 출력


int(3.2) ### 반환
괄호안을 정수로 바꿈
소수점버림


float(3) ### 반환
괄호안을 실수로 바꿈
부동소수점(floating point)


complex(1.2, 4.1) ### 반환
두 실수를 복소수로 만듦


del x, y
변수삭제


s = input('문자열입력하세요:') ### 반환
사용자의 입력을 변수에 저장


a,b = map(float, input('숫자두개:').split()) ### 반환
split의 결과를 모두 float으로 변환


print('hello', 'python', sep='')
값 사이에 공백없이
(sep는 구분자라는 뜻의 separator에서)


print(1,2,3,end='')
end는 명시해주지않는이상 
기본적으로 end='\n'로 되어있음


divmod(5,2) ### 반환
몫과 나머지를 튜플로 반환


id(1.0) ### 반환
id는 객체의 고유한 값(메모리 주소)을 구합니다.(이 값은 파이썬을 실행하는 동안에는 계속 유지되며 다시 실행하면 달라집니다.)


bool('False') ### 반환
정수, 실수, 문자열을 불로 만들 때 사용하며, 문자열의 내용자체는 판단하지 않으며 값이 있으면 True


for i in range(10):           # 0부터 9까지 반복
for i in range(5, 12):        # 5부터 11까지 반복
for i in range(0, 10, 2):     # 0부터 8까지 2씩 증가
for i in range(10, 0, -1):    # 10에서 1까지 역순으로 숫자 생성
for i in reversed(range(10)): #숫자의 순서를 반대로 뒤집음


list(range(10,0,-1)) ### 반환
괄호안을 리스트로 만들어줌


len([a,b,c]) ### 반환
요소의 개수(길이)를 리턴


tuple() ### 반환
괄호안은 튜플로 만들어줌


enumerate(리스트) ### 반환
반복문에서 인덱스와 요소의 값을 동시에 꺼내 올 수 있습니다.


min(리스트(튜플)) ### 반환
리스트(튜플)에서 가장 작은 값을 구한다.


max(리스트(튜플)) ### 반환
리스트(튜플)에서 가장 큰 값을 구한다.


sum(리스트(튜플)) ### 반환
리스트(튜플)의 요소의 합계를 구한다.


a = list(map(int, a)) ### 반환
리스트a의 모든 요소를 int를 사용해서 변환합니다.


zip(x,y) ### 반환
x와 y가 리스트(시퀀스)일때
요소끼리 짝을 지어줌(길이안맞으면 적은쪽기준)


sorted(반복가능객체, key=정렬함수, reverse=T/F)
2차원 리스트를 정렬할 때 사용
코드참고


set(a)
괄호안 시퀀스 자료형을 집합으로 만들어줌
& 교집합, | 합집합, ^ xor집합?, - 차집합


a =frozenset(range(10))
내용을 변경할 수 없는 세트
frozenset는 세트 안에 세트를 넣을 수 있다.


